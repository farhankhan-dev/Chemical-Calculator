import 'package:flutter/material.dart';

/// App color palette — purple/violet primary matching reference UI
class AppColors {
  AppColors._();

  // Primary palette
  static const Color primary = Color(0xFF6C3CE1);
  static const Color primaryLight = Color(0xFF8B5CF6);
  static const Color primaryDark = Color(0xFF5B21B6);
  static const Color primarySurface = Color(0xFFF3EEFF);

  // Accent
  static const Color accent = Color(0xFF7C4DFF);

  // Backgrounds
  static const Color background = Color(0xFFF8F9FA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color cardBackground = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimary = Color(0xFF1A1A2E);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textTertiary = Color(0xFF9CA3AF);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Borders
  static const Color border = Color(0xFFE5E7EB);
  static const Color borderLight = Color(0xFFF3F4F6);

  // Status
  static const Color success = Color(0xFF10B981);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
  static const Color info = Color(0xFF3B82F6);

  // Splash screen gradient
  static const Color splashStart = Color(0xFF6C3CE1);
  static const Color splashEnd = Color(0xFF8B5CF6);

  // Chemical detail header gradient
  static const Color headerStart = Color(0xFF6C3CE1);
  static const Color headerEnd = Color(0xFF9333EA);

  // Shadows
  static const Color shadow = Color(0x1A000000);
  static const Color shadowLight = Color(0x0D000000);
}
